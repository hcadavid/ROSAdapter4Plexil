Command TakePicture;
Command Move(Integer);
Command pprint(...);

Example:{
    Boolean end=false;
    
    Concurrence{
        Alert:{
            StartCondition Lookup(Temperature)>500;
            pprint ("!!!!!!OVERHEATING!!!!");
        }

        NodeOne:{
            while (Lookup(Temperature)<100){                
                TakePicture();
            }               
            end=true;
        }

        NodeTwo:{
            StartCondition end==true;
            Move(10);    
        }

    }

}